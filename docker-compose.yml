version: "2.0"

services:
  balancer:
    build: "balancer"
    image: "abrarov/haproxy-test-balancer"
    logging:
      driver: "json-file"

  backend:
    build: "backend"
    image: "abrarov/backend-test-backend"
    logging:
      driver: "json-file"
